name: Deploy to nlo.gg

on:
  workflow_dispatch:

jobs:
  skipped:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Live nlo.gg now deploys from dpztxzx9nf-lab/minecraft-workspace apps/nlo-hub."
          echo "This workflow is a no-op. Do not git-reset /opt/nlo/app from this repo."
